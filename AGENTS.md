# AGENTS.md

## 项目目标

本仓库用于开发“校园体育赛事直播与点播平台”。首要目标是提供可重复运行的最小可用版本：Windows 上的 OBS 通过 RTMP 推流，服务端将直播转为浏览器可播放的 HLS；Web 客户端提供首页、直播列表、直播播放、点播列表和点播播放页面。

所有实现优先考虑简单、稳定、可验证和便于团队接手。未经实际需求证明，不提前引入微服务、消息队列、Kubernetes 或复杂权限系统。

## 目录与文件管理

- `apps/web/`：React + TypeScript Web 客户端。
- `apps/api/`：FastAPI 业务 API。
- `infra/`：SRS、Nginx 等服务配置。
- `storage/`：本地运行数据；只提交占位文件和说明，不提交媒体与运行产物。
- `scripts/`：环境检查、启动、停止和验证脚本。
- `docs/`：架构、开发、测试、推流和协作文档。
- `tests/`：跨服务或端到端测试；服务单元测试放在对应应用目录。

新增文件必须放入职责清晰的目录。源代码不得依赖开发者机器上的绝对路径。大型视频、HLS 分片、日志、缓存、SQLite 文件、构建产物和依赖目录必须保持在 Git 之外。

## Git 规范

- 主分支名为 `main`，必须保持可运行。
- 功能分支使用 `codex/feature-<topic>`，修复分支使用 `codex/fix-<topic>`，文档分支使用 `codex/docs-<topic>`。
- Commit 遵循 Conventional Commits：`feat:`、`fix:`、`docs:`、`test:`、`refactor:`、`chore:`。
- 每个 commit 只表达一个完整意图，信息使用英文祈使语气，不允许 `update`、`changes`、`tmp` 等无意义说明。
- 禁止强推共享分支、改写他人已发布历史、提交秘密或大文件。
- 合并前同步 `main`、解决冲突并通过适用检查；优先使用 pull request 和审查。

## 团队协作

- 开始工作前先读本文件、`README.md` 和相关 `docs/`。
- 修改接口、端口、目录、环境变量或启动方式时，同一变更必须更新文档和 `.env.example`。
- 不覆盖无法确认来源的未提交改动。发现冲突时保留现场并说明。
- Pull request 要说明问题、最终行为、验证命令和已知限制。
- 代码评审关注可运行性、兼容性、错误处理、秘密泄露和媒体文件误提交。

## 测试要求

- 每次修改至少运行与改动直接相关的测试和静态检查。
- API 改动运行 `pytest`；Web 改动运行类型检查、单元测试和构建。
- Docker 或配置改动运行 `docker compose config`，可用时执行容器健康检查。
- 直播链路改动必须验证 OBS/FFmpeg 推流到 HLS 播放；点播链路改动必须验证 Range 请求、播放、暂停和拖动。
- 修复缺陷时优先补充能复现根因的测试。不得通过跳过失败测试来完成任务。

## Docker 规则

- `compose.yaml` 是本地完整环境的唯一入口。
- 镜像版本需要显式声明；升级版本必须记录原因并重新验证。
- 容器内配置由仓库文件或环境变量提供，秘密只放在未提交的 `.env`。
- 数据使用命名卷或 `storage/` 挂载，不写入镜像层，不提交运行数据。
- Dockerfile 保持小而可重复构建；不得在镜像中保存凭据。
- 服务必须配置合理的健康检查、重启策略和日志输出到标准输出。

## 文档维护

- `README.md` 保持新成员从 clone 到启动的最短路径。
- `docs/architecture.md` 记录组件职责、数据流和重要决策。
- `docs/development.md` 记录环境、命令和调试方式。
- `docs/streaming.md` 记录 OBS 参数、直播地址和故障排查。
- `docs/testing.md` 记录各层测试与验收步骤。
- 文档中的命令必须在项目根目录可执行。行为变化与文档更新必须进入同一个 PR。

## 禁止事项

- 禁止提交 `.env`、密码、Token、私钥和真实生产地址。
- 禁止提交大型视频、HLS 分片、日志、数据库文件、`node_modules`、虚拟环境和构建产物。
- 禁止直接修改第三方镜像内部文件来形成不可复现状态。
- 禁止以关闭安全校验、吞掉异常、硬编码机器路径或删除失败测试来绕过问题。
- 禁止在没有验证的情况下宣称功能可用。
- 禁止为尚未出现的扩展需求引入复杂基础设施。

## Codex 执行原则

- Codex 应主动检查、实现、测试、修复和记录，不只提供教程或方案。
- 普通技术选择由 Codex 依据最小复杂度和可维护性自行决定。
- 操作前检查 Git 状态和现有文件；保护团队成员未提交的工作。
- 每个阶段先完成可审查的实现，再运行对应验证，再创建语义清晰的 commit。
- 遇到失败应定位根因；若受外部环境阻塞，记录已验证事实、复现命令和解除阻塞后的下一步。
- 只有需要凭据、创建公开仓库、删除重要数据、覆盖他人工作或存在重大需求冲突时才向用户询问。

- Docker 测试任务必须显式重建相关测试镜像，避免验证旧代码。远程 CI 与本地测试结果分别记录，不得混称。
- 环境修复记录必须反映实际使用的 Docker 运行方式；代理和凭据配置仅保存在主机，不入库。
